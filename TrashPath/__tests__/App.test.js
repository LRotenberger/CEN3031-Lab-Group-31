import React from 'react';
import renderer from 'react-test-renderer';

import GeotagSubmit from '../components/GeotagSubmit';

//Quick setup for unit testing

describe('GeoTag Functionality', () => {
  it('GeoTag is Successfully Stored', () => {
    
    expect(1).toBe(1);
  });

  it('Camera Accessed', () => {
    expect(1).toBe(1)
  });

  it('Checkmark Visible', () => {
    expect(1).toBe(1);
  });
});